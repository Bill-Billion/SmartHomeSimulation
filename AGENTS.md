# AGENTS.md

## 基础约定

- 作者固定为 `Bill Billion`
- 面向用户的解释、文档和界面文案优先使用中文
- 禁止臆测。遇到信息缺失时，要先核对事实，再更新计划或实现
- 旧的多智能体架构文档是历史讨论稿，当前实现以 `docs/floorplan-to-3d-minimum-implementation.md` 为准

## 计划与记忆

- 项目阶段计划和完成度的专用记忆文件是 `docs/project-stage-todo.md`
- 只要计划、阶段目标、优先级或任务状态发生变化，就必须在同一轮改动里同步更新 `docs/project-stage-todo.md`
- 任务完成后，如果状态从未完成变为已完成，也必须同步更新 `docs/project-stage-todo.md`
- 计划更新、阶段判断更新、质量复核结论更新后，都要同步写入项目记忆

## 完成前校验

任何任务在标记完成前，都必须满足以下条件：

- 已确认任务意图理解正确，没有带着误解实现
- 代码设计合理，关键链路具备明确的异常处理、降级和可维护性
- 相关测试可以正常通过
- 每完成一部分代码工作后，都要调用代码优化专家智能体，对本轮改动及相关代码做一次质量评估
- 代码优化专家的评估结论要写入项目记忆；如果发现明显问题，先修正，再决定是否标记完成

如果当前任务没有可执行的自动化测试，必须在最终说明中写清：

- 为什么没有自动化测试
- 做了什么人工验证
- 还剩什么风险

## 文档同步

- 任何影响计划、实现边界或阶段状态的变更，都要同步更新相关文档
- 如果文档由 `Bill Billion` 维护且发生正式变更，按现有体系递增小版本号
